# Branch review corrections

The first review corrections address four reproduced defects:

- Source reads share an authorization checkpoint for at most the existing two-second polling interval. Inventory and publication still request a fresh checkpoint, stale concurrent reads share one pending check, and abort/disposal closes the context. The 1,000-file regression models 65 ms database latency and requires two checkpoints instead of one per source file.
- Straight-line implementation matching stops after known terminal statements. Unreachable local calls no longer support request validation, authentication, route/service, or form-validation evidence.
- Cleanup evidence requires an immediate, unconditional release in `finally` and a reachable direct query in the corresponding `try`. Conditional cleanup and preceding effects that could bypass release are outside the supported pattern.
- Prisma declarations use Prisma comment/string handling. Commented-out models do not support data-modeling evidence, and apostrophes in comments do not invalidate real models. Malformed or unsupported lexical syntax fails closed; SQL handling remains separate.

Corrected analyzer profiles have new version identities. Existing observations and historical migrations remain unchanged. The additive migration registers the new definitions and validates coherent old or new version combinations. New workers reject jobs pinned to unsupported old execution policies before processing source; create a new analysis to use the corrected implementation.

The aggregation process verifier extracts fresh synthetic evidence under the current profiles, checks independent arithmetic and ordering invariance, and retains its 20,000-observation workload, 384 MiB heap, and 30-second watchdog. Historical Run 11 examples stay unchanged and are explicitly rejected as input to the corrected analyzer.

Production and development dependencies were updated within their existing major versions, including Next.js and Vitest security patches. The pinned extraction/parser dependencies remain unchanged.

The follow-up review corrections address four additional reproduced defects:

- All implementation detector emissions share bounded lexical reachability checks. Dead ownership/error guards, database calls, retry loops, AI calls, route registrations, JSX, and test assertions no longer support claims or test corroboration. Transaction credit requires at least two reachable direct awaited writes. Hoisted function declarations remain supported within reachable scopes; dead function creation and unreachable initializers are excluded.
- Request-state evidence requires direct pending setup immediately before the request's `try`, an awaited fetch as its first effect, and immediate unconditional error/reset setters in `catch`/`finally`. State rendered only in unreachable JSX cannot establish displayed pending/error state.
- `var` bindings are installed in their containing function, module, or class static block, including nested destructuring. A block or loop `var` shadow cannot resolve to an imported library; block-scoped bindings and declarations in other functions retain their separate scopes.
- Comparisons include implementation limits and capability assessability in scope checks. Either scan's incomplete scope qualifies every evidence change and numeric delta, including equal limit counters that cannot establish identical assessed files. The database-backed regression adds 256 unrelated files and proves that unchanged retry source displaced by the detector budget is reported with limited comparability.

The corrected implementation detector bundle is `1.0.2`; structural extractor `1.0.1` and coverage manifests `1.1.1`/`1.2.1` are unchanged. An additive migration registers the new detector definitions while preserving both historical detector versions. Mixed-version snapshots are rejected, and workers reject earlier execution policies before source acquisition. Comparison responses use `evidence-diff-1.0.1`; the contract also accepts the historical comparison version.

Historical human approvals in the Run 16 review/rendering files remain intact. They are bound to the earlier domain implementation, so the corrected implementation has no transferred human labels. Automated benchmarking may continue, but current human-review metrics remain unavailable and release stays on hold until the required reviews and external gates are satisfied.

Verification results are recorded in `run16-verification.json`, `run16-results.json`, and `run16-load.json`. These cover local synthetic behavior, not live providers or deployment readiness.

The completed verification passes all 22 checks against one unchanged implementation digest: 83 contract tests, 1,338 backend tests, 109 PostgreSQL tests, 580 frontend tests, coverage thresholds, builds, process limits, and browser journeys. The first review's application dependency audits reported zero production or development vulnerabilities; this follow-up does not change dependencies. The recorded rollout decision remains HOLD with nine pending gates, including a new bound human review of the corrected implementation.

The updated Next/Sentry proxy emits a nonblocking close-listener threshold warning. A minimal reproduction without Express or rate limiting found 11 listeners per response; all five response objects were collected after completion in WeakRef/GC probes. No retained-response accumulation was reproduced, and no warning suppression or application workaround was added.

## September 22 privacy and webhook corrections

- Browser telemetry drops all breadcrumbs created on readiness screens and navigation breadcrumbs entering or leaving those screens, including URLs with fragments. Replay blocking alone does not suppress repository names in click breadcrumbs. A regression uses the actual browser SDK with a local transport to verify that a private repository click cannot reach an error sent after navigation to settings; ordinary settings breadcrumbs still work.
- GitHub webhook invalidation now shares the same affected repository or installation scope for grants and discovery locators. Additions, creation, and unsuspension preserve active selections' locators. Repository events invalidate only the affected repositories; installation-wide events still revoke all scoped access. Authorization revocation removes only that account's discoveries. Revoked grants remain revoked until fresh verification and selection.

Deploy the additive migration `20260922000100_scoped_github_webhook_invalidation.sql` through the existing migration runner. Earlier migration files are unchanged. If an earlier webhook already deleted discovery locators, refresh repository discovery to rebuild them; the migration does not reconstruct deleted encrypted data.

Before these fixes, the regression checks reproduced four frontend failures and seven PostgreSQL failures. After the fixes, verification on Node 22.23.2 passed all 1,338 backend tests, 583 frontend tests, and 118 PostgreSQL tests, plus both application typechecks and frontend lint. The PostgreSQL checks include delivery replay, authorization races, account isolation, full installation revocation, and browser-role denial of webhook execution. The earlier Run 16 verification artifacts were not regenerated for this change.

## September 23 binding, comparison and key-rotation corrections

- Named class expressions bind their names inside the class, including methods and field initializers. A local `class Pool` no longer resolves to an outer `pg` import. Positive controls retain genuine library calls outside that class.
- Assignment analysis follows nested object/array targets, defaults, rest targets, shorthand properties, aliases and `for…of`/`for…in` assignment targets within the existing traversal budget. Reads in defaults or computed property names do not invalidate unrelated objects. Replacing a client's `query` method through destructuring no longer supports parameterized-query evidence or security capability strength.
- Comparison scope includes rejected alias configurations, unresolved imports, ambiguous bindings and dynamic references. Either side's unresolved scope limits interpretation even when the counters match. The database-backed regression changes only an inherited `tsconfig` configuration and proves that the unchanged query implementation's lost observation and −0.55 strength delta are qualified by incomplete/changed scope.
- Production API and worker factories support retained decryption keys through `EVIDENCE_LOCATOR_READ_KEYS`. Only the active key writes new ciphertext/fingerprints. Configuration is bounded, validates independent key pairs and rejects duplicate/active version collisions without exposing values. Tests cover old locators and pinned branches, overlapping process configurations, restarts, removal of read keys and unchanged owner/revocation checks. The operations runbook documents the deployment sequence.

Detector bundle `1.0.3` records the corrected binding semantics; structural extractor `1.0.1` and coverage manifests `1.1.1`/`1.2.1` remain unchanged. Apply the additive migration `20260923000100_detector_binding_versions.sql` and rebuild shared contracts in both applications before switching the worker release. All three earlier detector definitions and sealed reports remain valid. Workers reject earlier execution policies before acquiring source, so drain compatible work or cancel it through the authorized API as described in the runbook. Comparison responses now identify `evidence-diff-1.0.2`; older response versions remain accepted.

The regression suite reproduced 35 failures before implementation. After the fixes, all 127 focused tests and 119 PostgreSQL tests passed, including old-version persistence, mixed-version rejection and complete rollback. Final review covered the implementation, migration, contracts, regression quality, configuration errors and deployment compatibility. Human approval artifacts remain bound to the earlier implementation; no approval was transferred and no deployment, provider call or feature activation was performed.

Full verification on Node 22.23.2 passed all 22 checks against one unchanged implementation digest: 83 contract tests, 1,385 backend tests, 119 PostgreSQL tests, 583 frontend tests and 15 HTTP/browser checks, plus coverage thresholds, typechecks, lint, builds and process limits. The frozen detector benchmark retains 24 true positives, zero false positives and its one documented false negative; all 13 synthetic load jobs completed with no terminal workspaces remaining. Verification, benchmark, load, preflight and release-decision artifacts were refreshed. The recorded rollout decision remains HOLD with the same nine pending human and external gates.

## September 23 SQL screening and state-guard corrections

- SQL screening recognizes quoted strings, quoted identifiers and dollar-quoted bodies before stripping actual comments. Comment markers inside literals cannot hide later data statements. Stored SQL remains screened, recursive body inspection is bounded, and unterminated quoting fails closed. Ingestion tests prove that affected schema/migration files never receive an extractor read capability, while schema-only defaults containing comment markers remain eligible.
- State-guard evidence requires a rejecting exit with no operation call: a bare return, primitive literal, or unshadowed `Error` construction with literal arguments. Returned/awaited calls, calls inside thrown expressions, and other unproven effects cannot establish the guard. The report regression verifies zero concurrency strength and no verified concurrency claim when both branches call the same operation.

Security policy `1.1.1` has a distinct ingestion hash, preventing reuse of snapshots screened by the earlier policy. Detector bundle `1.0.4` records the corrected guard semantics. Apply `20260923000200_sql_screening_and_state_guards.sql` before deploying the updated contracts/API/workers. Earlier migrations and sealed results are unchanged; earlier detector coverage remains readable, mixed versions are rejected, and incompatible queued policies fail before source acquisition. Use a fresh authorized analysis for corrected results.

Before implementation, the new regression cases reproduced 22 failures. The focused suite passed all 187 checks after the corrections, including ingestion, scoring, narrative, version identity and worker policy rejection.

Full verification on Node 22.23.2 passed all 22 checks against one unchanged implementation digest: 83 contract tests, 1,424 backend tests, 120 PostgreSQL tests, 583 frontend tests and 15 HTTP/browser checks, plus coverage thresholds, typechecks, lint, builds and process limits. The frozen benchmark retains 24 true positives, zero false positives and its one documented false negative; all 13 synthetic load jobs left no terminal workspaces. Automated verification and rollout-decision artifacts were refreshed. Rollout remains HOLD pending the same nine human and external gates; these checks made no live provider calls or deployment changes.
