// Stub — will be implemented when GitHub API integration is built
export async function getGitHubProfile(
  username: string,
  token: string
): Promise<Record<string, unknown> | null> {
  return null;
}
